<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:local="clr-namespace:HW6_2"
             xmlns:const="clr-namespace:Constants"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="HW6_2.MainPage">
    
<!--
    <ListView ItemsSource="{x:Static local:PagesViewModel.All}"
              ItemSelected="OnListViewItemSelected">
    <ListView.ItemTemplate>
            <DataTemplate>
                <TextCell Text="{Binding Title}"
                          Detail="{Binding Description}" />
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
-->
    <StackLayout>
        <Button Text="To Grid Page" Clicked="GridClicked" BackgroundColor="{x:Static local:PagesViewModel.ColorA}"/>
        <Button Text="To List Page" Clicked="ListClicked" BackgroundColor="{x:Static local:PagesViewModel.ColorA}"/>
        <Button Text="To Scroll Page" Clicked="ScrollClicked" BackgroundColor="{x:Static local:PagesViewModel.ColorA}"/>

    </StackLayout>
</ContentPage>
